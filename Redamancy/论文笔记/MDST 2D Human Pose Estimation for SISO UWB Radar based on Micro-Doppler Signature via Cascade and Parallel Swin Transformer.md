这篇论文的背景源于近年来智能无线感知系统在复杂城市环境中的迅猛发展，研究人员致力于通过无线信号来感知和理解人类行为。这类系统在自动驾驶、国防监控、以及老年和残疾人辅助等多个领域中展现了巨大的应用潜力。论文提出了一个新颖的基于单输入单输出超宽带雷达(SISO UWB Radar)的2D人体姿态估计方法，利用微多普勒(MD)特征结合层叠与并行Swin Transformer网络实现精确的人体姿态识别。

提出这一研究的原因包括现有雷达成像技术在环境变化下的不稳定性，以及现有数据集在标注和雷达成像质量方面的局限性。为了克服这些挑战，研究引入了一个新的数据集（HPSUR），并设计了一个基于MD特征的Swin Transformer网络，旨在提高人体姿态估计的精确度和鲁棒性。

这篇论文确实提到了隐私问题。当前的摄像头基于的人体姿态估计算法在隐私保护方面存在挑战，因为摄像头监控会涉及到个人隐私的泄露问题。此外，摄像头的监控数据可能会被黑客攻击，导致敏感信息的泄露。相比之下，基于雷达的感知系统由于其强大的穿透能力和对环境因素（如衣物、背景、光照和遮挡）的低敏感性，被认为在隐私保护方面具有优势。这些系统不依赖于视觉信息，因此可以避免摄像头监控带来的隐私问题，提供了更为安全和可靠的人体姿态估计方法。

反卷积层（Transposed Convolution Layer），有时也称为反向卷积层或上采样层，主要用于卷积神经网络（CNN）中进行特征图的上采样（即增大特征图的空间尺寸）。它的主要作用包括以下几点：

1. **上采样（Upsampling）**：反卷积层的主要作用是将低分辨率的特征图恢复为更高分辨率的特征图，通常在图像生成、超分辨率任务或者语义分割任务中使用。通过学习到的权重，反卷积层可以将输入的特征图转换为更大的输出特征图，同时保持空间结构的信息。

2. **恢复空间信息**：在某些任务（如图像重建）中，原始卷积操作会对输入特征图进行下采样（通过池化或卷积），从而丢失一部分空间信息。反卷积层可以恢复这些被丢失的信息，重新生成输入图像或生成更高分辨率的输出图像。

3. **学习上采样**：与固定的上采样方法（如双线性插值）不同，反卷积层是一个可学习的层，能够通过反向传播调整其参数，以优化生成结果。因此，它在恢复细节和生成逼真的高分辨率输出方面具有更好的表现。

4. **配合卷积层使用**：在编码器-解码器结构（如 U-Net 或 GANs 中的生成器）中，反卷积层通常用于解码器部分，将经过卷积和池化处理的低分辨率特征图逐步上采样到与输入图像相同的分辨率。

总之，反卷积层的主要作用在于通过学习的方式对特征图进行上采样和空间信息恢复，是卷积神经网络中常用的工具，尤其在图像生成和语义分割等任务中表现尤为重要。
***
![[Pasted image 20240828200410.png]]
微多普勒光谱图
***
论文提出了一种基于**SISO（单输入单输出）超宽带（UWB）雷达**的二维人体姿态识别方法，利用了**微多普勒（Micro-Doppler, MD）特征**，通过**层叠和并行的Swin Transformer** 网络结构来实现对人体姿态的精确估计。以下是论文的识别姿态的根本方法的概述：

### 1. **数据预处理：**
- 首先，对雷达回波数据进行预处理，包括直接波抑制和自适应滤波，以去除杂波，改善信噪比。
- 然后使用**短时傅里叶变换（STFT）**生成人体姿态的微多普勒光谱图。这些光谱图作为模型的输入，反映了人体各个部位运动所产生的频率变化。

### 2. **MD特征提取：**
- 将生成的微多普勒光谱图嵌入成若干小的图块（patches），这些图块作为Transformer模型的输入。
- 在此基础上，引入了绝对位置嵌入（absolute positional embedding）来增加输入的位置信息，有助于在训练过程中捕捉姿态特征。

### 3. **使用Swin Transformer进行姿态估计：**
- 论文设计了一种**微多普勒Swin Transformer（MDST）**网络，用于从MD光谱图中提取人体姿态信息。该网络包含了四个顺序连接的阶段，每个阶段进一步细化和转换MD特征。
- 通过引入**窗口多头自注意力（W-MSA）**和**移位窗口多头自注意力（SW-MSA）**，网络分别处理MD光谱图中的内帧（inner-frame）特征和帧间（intra-frame）特征。

### 4. **层叠和并行网络结构：**
- MDST网络在原始Swin Transformer基础上设计了**层叠（Cascade）和并行（Parallel）Swin Transformer模块**，以增强本地和全局特征的提取能力。
  - **层叠结构（Cascade Swin Transformer）**：用于逐层细化特征表示，适合提取更高层次的姿态特征。
  - **并行结构（Parallel Swin Transformer）**：用于同时处理多尺度的MD特征，优化对不同分辨率下的姿态估计。

### 5. **反转特征金字塔网络（IFPN）：**
- 在MDST之后，引入了一个**反转特征金字塔网络（IFPN）**来进行多尺度特征融合。该网络通过多层特征图的上采样和融合，增强了对复杂姿态的识别能力。

### 6. **解码与姿态预测：**
- 使用解码器模块对特征进行反卷积和全连接操作，以生成人体各关键点的热图（heatmap），然后通过argmax操作提取每个关键点的二维坐标。

### 总结
论文的根本方法是利用SISO UWB雷达捕获的人体运动微多普勒特征，通过MDST网络结构（包括层叠和并行Transformer模块）以及IFPN进行多尺度特征融合，最终生成人体关键点的二维坐标，实现人体姿态的精确估计。

***
[nesl/RadHAR: Human Activity Recognition from Point Clouds Generated through a Millimeter-wave Radar (github.com)](https://github.com/nesl/RadHAR/tree/master)
这是由SISO超宽带（UWB）雷达采集的原始数据与训练的一个项目
***
在学术界中，雷达原始数据的可视化主要围绕着展示目标运动特征和信号特性，常用的方法包括以下几种：

### 1. **距离-时间图（Range-Time Plot）**

- **方法描述**：距离-时间图用于展示目标在不同时间点上的距离变化，是学术界中常用的雷达数据可视化方法之一。
- **用途**：主要用于分析目标的运动行为，例如目标的距离变化、速度变化等。在研究动态目标检测与跟踪时，尤其有效。
- **实现方法**：
    - 在快时间（fast-time）轴上对雷达回波信号进行采样，通过慢时间（slow-time）轴上的多个脉冲来观察目标的距离变化。
    - 绘制二维图像，其中x轴代表慢时间，y轴代表快时间，颜色或灰度级别表示回波信号的强度或反射强度。

### 2. **距离-多普勒图（Range-Doppler Map）**

- **方法描述**：距离-多普勒图通过在快时间域上执行傅里叶变换生成，展示了目标在不同距离和多普勒频率（速度）上的反射信号强度。
- **用途**：广泛用于目标检测、速度估计、目标分类等研究。它能够同时展示目标与雷达的相对距离和速度信息。
- **实现方法**：
    - 对每个快时间脉冲序列使用快速傅里叶变换（FFT）来生成距离单元。
    - 在慢时间上对多个距离单元执行傅里叶变换，以得到目标的多普勒频谱。
    - 绘制二维图像，x轴为距离（range），y轴为多普勒频率，颜色表示反射强度。

### 3. **微多普勒频谱图（Micro-Doppler Spectrogram）**

- **方法描述**：微多普勒频谱图是使用短时傅里叶变换（STFT）将原始雷达信号投射到时间-频率域中的一种可视化方法，能够展示由目标微动（例如人体的四肢运动）引起的多普勒频率变化。
- **用途**：常用于识别人类活动、手势识别、无人机识别等领域。该图能够揭示目标运动中的细微变化。
- **实现方法**：
    - 将每个雷达回波信号分段，使用短时傅里叶变换（STFT）或连续小波变换（CWT）生成微多普勒图。
    - 绘制二维图像，x轴为时间，y轴为频率，颜色或灰度级别表示频率处的反射强度。

### 4. **距离-角度图（Range-Angle Map）**

- **方法描述**：距离-角度图用于展示目标在不同角度和距离下的回波强度。
- **用途**：用于多目标跟踪、角度估计、目标分辨等。主要应用于MIMO（多输入多输出）雷达系统。
- **实现方法**：
    - 对雷达回波信号进行波束形成（Beamforming）处理。
    - 绘制二维图像，x轴为距离，y轴为角度，颜色表示反射强度。

### 5. **时频分析图（Time-Frequency Analysis Plot）**

- **方法描述**：使用时频分析技术（例如STFT或Wigner-Ville分布）来同时表示时间和频率信息，突出频率分量随时间的变化。
- **用途**：用于分析复杂目标的动态行为（如人类走路、跑步等）或者识别特定的目标活动模式。
- **实现方法**：
    - 通过STFT、CWT或Wigner-Ville分布将雷达回波信号转化为时频图。
    - x轴为时间，y轴为频率，颜色表示频谱强度。

### 6. **三维点云图（3D Point Cloud Map）**

- **方法描述**：三维点云图用于可视化雷达数据中目标的三维位置分布，通过显示目标在x、y、z坐标系中的位置。
- **用途**：用于识别和分类复杂目标形状和运动特征（如自动驾驶中的行人和车辆检测）。
- **实现方法**：
    - 提取每个雷达回波的三维坐标（x, y, z）信息，生成三维散点图。
    - 使用3D可视化工具（如Matplotlib的`plot3D`或PCL库）绘制。

### 总结

学术界常用的雷达数据可视化方法主要包括距离-时间图、距离-多普勒图、微多普勒频谱图、距离-角度图、时频分析图和三维点云图。这些方法各有用途，具体选择取决于研究的目标和数据的特征。可视化结果能够帮助研究人员更好地理解和分析雷达数据中的目标行为和信号特性。
***
SISO超宽带（UWB）雷达采集的原始数据通常包含一系列反映目标和环境信息的测量值。这些数据的格式会根据雷达制造商和应用程序的具体实现而有所不同，但典型的原始数据格式通常包括以下几种关键字段：

### 1. **序列号（seq）**
- 表示数据包的序列编号，用于识别和排序多个雷达数据帧，确保数据的顺序处理。

### 2. **时间戳（timestamp）**
- **秒（secs）**和**纳秒（nsecs）**：两个字段结合形成完整的时间戳，表示每个数据包被采集的精确时间。时间戳是慢时间（slow-time）的基础，用于测量不同雷达脉冲之间的时间间隔。

### 3. **点ID（point_id）**
- 每个目标点或反射点的唯一标识符，帮助区分在同一帧中的多个目标或反射点。

### 4. **三维坐标（x, y, z）**
- 目标点在雷达坐标系中的三维空间位置，通常以米为单位。
  - **x**: 水平位置坐标。
  - **y**: 垂直位置坐标。
  - **z**: 深度或高度坐标。

### 5. **距离（range）**
- 雷达到目标的距离（以米为单位）。这是基于回波信号的飞行时间（Time of Flight, ToF）计算得出的，通常是目标距离估计的一个直接量度。

### 6. **速度（velocity）**
- 目标相对于雷达的径向速度（以米/秒为单位）。通过多普勒频移计算得到，反映目标的移动方向和速度。

### 7. **多普勒频谱分箱值（doppler_bin）**
- 表示多普勒频谱中的分箱值，用于识别不同频率范围内的信号强度。用于目标速度的估计。

### 8. **方位角（bearing）**
- 表示目标相对于雷达的方位角（以度为单位），用于测量目标的横向位置或方向。

### 9. **信号强度（intensity）**
- 表示雷达回波的信号强度或功率（通常以dB为单位），反映目标反射的雷达波强度，通常用于目标检测和分类。

### 10. **帧ID（frame_id）**
- 一个字符串或数字标识符，用于标识当前数据所属的雷达帧。多个数据点可以属于同一帧。

### **数据格式的结构**
这些字段通常以一种结构化的格式存储，例如CSV、JSON或二进制文件。以文本格式表示时，每行数据可能表示一个单独的雷达点，包含上述所有字段。以下是一个典型的原始数据条目的示例：

```
seq: 102, stamp: { secs: 1618709984, nsecs: 123456789 }, frame_id: "ti_mmwave", 
point_id: 1, x: 0.5, y: -0.2, z: 1.5, range: 5.0, velocity: 0.3, doppler_bin: 32, 
bearing: 45.0, intensity: -75.0
```
***
在雷达系统中，**快时间（fast-time）**和**慢时间（slow-time）**是用于描述信号采集和处理的两个重要概念，帮助我们理解雷达如何捕捉目标的位置信息和运动特征。

### 1. **快时间（Fast-Time）**
- **快时间**指的是每个雷达脉冲发出后，接收到目标反射信号的时间。
- 这个时间范围很短，通常是以纳秒或微秒为单位。雷达在发送一个脉冲后，会在非常短的时间内接收到从不同目标反射回来的信号。
- 快时间主要用来计算**目标的距离**。通过测量从雷达发射信号到回波信号返回的时间，可以确定目标的距离（例如：你发出声音后，听到回声的时间差越短，目标越近）。

### 2. **慢时间（Slow-Time）**
- **慢时间**指的是雷达在发出一系列连续脉冲之间的时间间隔。
- 这个时间范围较长，通常以毫秒或秒为单位。雷达发射多个脉冲来持续跟踪目标的运动。
- 慢时间用于分析**目标的速度和运动特征**。通过多次脉冲（每次都记录一个“快时间”数据），雷达可以检测到目标的速度变化（例如：观察汽车在一段时间内的加速或减速情况）。

### 举个例子：
假设你在黑暗中使用手电筒来找一个移动的物体：
- **快时间**就像你打开手电筒的一瞬间，光从手电筒发出并反射回来，这个时间非常短，你用它来估计物体距离。
- **慢时间**就像你每隔一秒钟闪一次手电筒，通过比较每次闪光的回声，你可以判断这个物体是向你移动还是离你远去。

### 总结：
- **快时间**用于测量单个脉冲内目标的距离。
- **慢时间**用于测量多个脉冲之间目标的速度和运动方向。 

这两个时间的结合让雷达系统能够全面地了解目标的静态位置和动态变化。
***
在雷达信号处理中，**直接波抑制**和**自适应滤波**是用来增强目标检测效果的两种信号处理技术，它们主要针对雷达信号中的噪声和干扰进行处理。

### 1. **直接波抑制（Direct Wave Suppression）**

- **直接波**指的是雷达天线发射的信号直接反射回接收器的那部分信号，这部分信号不包含任何关于目标的信息，因为它没有与目标物体发生任何反射或散射。
- **直接波抑制**的目的是去除或减少这部分直接回波的影响，以便更好地检测到来自目标的有用信号。

#### 如何进行处理：
- **时间窗过滤**：通过延迟和滤波，识别并过滤掉早期返回的强信号（通常是直接波），因为这些信号会在非常短的时间内返回到雷达接收器。
- **天线阵列技术**：使用多天线阵列的方向性来区分直接波和来自目标的反射波，或者利用相位和幅度差异来抑制直接波。
- **自适应算法**：采用自适应算法（如自适应自干扰消除）实时检测和抑制直接波信号，进一步减少其影响。

### 2. **自适应滤波（Adaptive Filtering）**

- **自适应滤波**是一种动态调整滤波器参数的方法，用于去除信号中的噪声和不相关干扰，增强感兴趣的目标信号。
- 它通过不断更新滤波器的系数，使其适应信号环境的变化，因此被称为“自适应”。

#### 如何进行处理：
- **噪声抑制**：自适应滤波器可以区分目标信号和噪声信号，通过不断调整滤波器参数，最大程度地保留目标信号的有用信息，并减少不相关的噪声。
- **回波增强**：通过自适应调整滤波器的频率响应，自适应滤波可以加强目标回波信号的特征，使目标信号更加明显。
- **典型方法**：
  - **LMS（最小均方）算法**：通过最小化输出误差的均方值，不断调整滤波器的系数，使得滤波器能够适应当前信号环境的变化。
  - **卡尔曼滤波**：利用统计模型预测和更新信号的状态，在具有较高噪声水平的情况下，更准确地跟踪目标。

### 总结：
- **直接波抑制**专注于消除不包含目标信息的强信号（直接波），防止其干扰有用信号的检测。
- **自适应滤波**则动态调整滤波器参数，以去除噪声和干扰，同时增强感兴趣的目标信号的特征。 
***
生成微多普勒（Micro-Doppler）光谱图的方法主要依赖于将雷达回波信号转换到时间-频率域，以捕捉目标物体（如人或车辆）细微运动（如步态、手势）的频率变化特征。以下是常用的几种方法：

### 1. **短时傅里叶变换（Short-Time Fourier Transform, STFT）**
- **方法描述**：STFT 是最常用的生成微多普勒光谱图的方法。它通过在短时间窗口内对信号进行傅里叶变换，得到信号在时间和频率上的联合表示。
- **实现步骤**：
  1. 将雷达回波信号分成重叠的短时间窗口（如20毫秒）。
  2. 对每个时间窗口内的信号进行傅里叶变换，得到该时间段内的频谱。
  3. 将所有窗口的频谱结果拼接，得到时间-频率域的微多普勒光谱图。
- **优点**：简单、直观，适用于平稳信号的分析。
- **缺点**：时间分辨率和频率分辨率的折衷（即窗口长度的选择影响两者的平衡）。

### 2. **连续小波变换（Continuous Wavelet Transform, CWT）**
- **方法描述**：CWT 使用可变大小的窗口函数来分析信号的不同频率成分，更适合非平稳信号（即信号频率随时间变化）。
- **实现步骤**：
  1. 选择一个母小波（如Morlet小波）和尺度范围。
  2. 对信号进行连续小波变换，得到信号在不同时间和频率尺度下的系数。
  3. 将小波系数的绝对值平方，得到时间-频率域的功率谱密度，即微多普勒光谱图。
- **优点**：能够提供高时间和频率分辨率，适合分析非平稳信号。
- **缺点**：计算复杂度较高，需要选择合适的母小波和尺度范围。

### 3. **Wigner-Ville分布（Wigner-Ville Distribution, WVD）**
- **方法描述**：WVD 是一种时间-频率分布方法，用于获取信号的高分辨率时间-频率表示。它通过计算自乘积来估计信号的瞬时频率。
- **实现步骤**：
  1. 计算信号的自相关函数。
  2. 对自相关函数的时间延迟变量进行傅里叶变换，得到Wigner-Ville分布。
  3. 绘制WVD图来显示信号的时间-频率特性。
- **优点**：提供高时间和频率分辨率，适用于短时间变化较大的信号。
- **缺点**：可能产生交叉项干扰（cross-terms），使得时间-频率图变得混乱。

### 4. **希尔伯特-黄变换（Hilbert-Huang Transform, HHT）**
- **方法描述**：HHT 结合经验模态分解（Empirical Mode Decomposition, EMD）和希尔伯特谱分析，适用于非线性和非平稳信号。
- **实现步骤**：
  1. 使用经验模态分解将信号分解为若干固有模态函数（IMFs）。
  2. 对每个IMF应用希尔伯特变换，计算其瞬时频率和瞬时幅度。
  3. 将所有IMFs的瞬时频率和幅度整合，得到信号的时间-频率能量谱，即微多普勒光谱图。
- **优点**：适用于非线性、非平稳信号，能够提供信号的本征特征。
- **缺点**：分解过程复杂，容易受到噪声影响。

### 5. **谱估计方法（Spectral Estimation Methods）**
- **方法描述**：通过现代谱估计技术，如自回归（Auto-Regressive, AR）模型、最小方差无畸变响应（Minimum Variance Distortionless Response, MVDR）方法来生成微多普勒图。
- **实现步骤**：
  1. 建立信号的数学模型（如AR模型）来估计其频谱。
  2. 根据估计得到的频谱，绘制时间-频率图。
- **优点**：在低信噪比环境下表现优异，能够获得更精确的频谱估计。
- **缺点**：依赖于模型的正确性，模型选择复杂。

### 6. **离散傅里叶变换（Discrete Fourier Transform, DFT）与快速傅里叶变换（Fast Fourier Transform, FFT）**
- **方法描述**：直接将雷达信号进行离散傅里叶变换（DFT）或快速傅里叶变换（FFT），用于分析固定时间窗口内的频率变化。
- **实现步骤**：
  1. 对信号进行分段。
  2. 对每一段应用DFT或FFT来获得其频谱。
  3. 组合所有段的频谱，形成微多普勒光谱图。
- **优点**：简单快速，易于实现。
- **缺点**：不适用于非平稳信号，时间和频率分辨率受限。

### 总结
生成微多普勒光谱图的方法有很多，选择合适的方法取决于信号特性（如平稳性、非线性）、分析目标（如频率分辨率、时间分辨率）和计算资源（如实时处理需求）。最常用的方法是短时傅里叶变换（STFT），但在处理非平稳信号时，小波变换（CWT）和希尔伯特-黄变换（HHT）可能更为适用。
***
在SISO超宽带（UWB）雷达数据中，`x`、`y`、`z`坐标的原点通常是雷达传感器的天线阵列位置，也就是雷达设备的安装位置。具体来说：

- **原点（0, 0, 0）**：通常位于雷达天线的中心或安装基座的位置。雷达系统会以这个点作为参考点来测量所有探测到的目标点的坐标。

- **坐标系定义**：
  - **`x` 轴**：一般表示前后方向。通常情况下，`x` 轴的正方向指向雷达正前方。
  - **`y` 轴**：一般表示左右方向。通常情况下，`y` 轴的正方向指向雷达的右侧（与`x`轴垂直）。
  - **`z` 轴**：一般表示上下方向。通常情况下，`z` 轴的正方向指向上方（垂直于地面）。

这些方向的定义会因雷达的具体型号和使用环境而有所不同。例如，如果雷达被安装在水平面上面朝前方，原点通常是雷达传感器的中心点，`x` 轴表示朝前的距离，`y` 轴表示左右的偏移量，`z` 轴表示高度或垂直方向的偏移量。
***
文件内容的结构如下（从之前的片段）：

```
x: 0.01171875
y: -0.0390625
z: -0.0078125
intensity: 30.7371826172
...
```

### 修改代码以确保正确解析文件：

```python
import matplotlib.pyplot as plt
from mpl_toolkits.mplot3d import Axes3D

# 文件路径
file_path = '___20_walk_1.txt'  # 替换为你的文件路径
x, y, z, intensity = [], [], [], []

with open(file_path, 'r') as file:
    current_point = {}
    for line in file:
        line = line.strip()  # 去除行首尾的空格和换行符
        if line.startswith('x:'):
            current_point['x'] = float(line.split('x:')[1].strip())
        elif line.startswith('y:'):
            current_point['y'] = float(line.split('y:')[1].strip())
        elif line.startswith('z:'):
            current_point['z'] = float(line.split('z:')[1].strip())
        elif line.startswith('intensity:'):
            current_point['intensity'] = float(line.split('intensity:')[1].strip())
        
        # 检查是否收集了完整的一个点的信息
        if len(current_point) == 4:
            x.append(current_point['x'])
            y.append(current_point['y'])
            z.append(current_point['z'])
            intensity.append(current_point['intensity'])
            current_point = {}  # 清空临时存储以准备下一次

# 确保数组中有数据
if x and y and z:
    # 创建3D图
    fig = plt.figure()
    ax = fig.add_subplot(111, projection='3d')

    # 绘制点云，使用颜色映射表示强度
    scatter = ax.scatter(x, y, z, c=intensity, cmap='viridis', marker='o')

    # 添加颜色条
    plt.colorbar(scatter, label='Intensity')

    ax.set_xlabel('X')
    ax.set_ylabel('Y')
    ax.set_zlabel('Z')
    plt.title('3D Point Cloud Visualization from UWB Radar Data')
    plt.show()
else:
    print("未能读取到任何数据点，请检查文件内容和格式。")
```
![[Pasted image 20240829142514.png]]
### 如何解读这个图：

1. **X, Y, Z坐标**：图中的`x`、`y`、`z`轴分别代表了数据在三维空间中的位置，反映了雷达探测到的目标物体在空间中的位置分布。
2. **颜色映射（Intensity）**：颜色条显示了强度值范围，图中的点根据其强度值（`intensity`）使用颜色编码。颜色越接近黄色，表示强度越高；颜色越接近紫色，表示强度越低。
3. **点云分布**：从图中可以看到，点云在特定区域有较高的密集度，这可能表示在该区域内有较多的反射物体或强回波信号。

可以理解为这张3D点云图显示的是在这 **0.18秒** 内采集到的所有雷达信号数据。

在这段时间内，雷达不断发射信号脉冲并接收回波，记录了在此时间窗口内检测到的所有物体的信息，如位置 (`x`, `y`, `z`) 和回波强度 (`intensity`) 等。这些数据点代表了雷达在这0.18秒内感知到的目标的空间分布和反射特性。

因此，这张图展示了在一个非常短的时间片段中（0.18秒）内的雷达探测结果，用于分析目标物体的瞬时状态或运动轨迹。雷达数据的快速采集和高频率更新非常适合用于动态物体检测和追踪。
***
在论文中，数据集的构建过程中提到的“帧”指的是基于SISO UWB雷达发射和接收信号后获得的一组数据。具体来说，每一帧数据通常对应于雷达在一个完整的发射-接收周期内捕获的所有回波信号的处理结果。

### 对“帧”的理解：

1. **帧的定义**：
   在雷达系统中，一帧通常表示一次完整的测量周期，包括雷达发射一个脉冲信号并接收到该信号的回波。在此期间，雷达探测器会收集到反射自目标的回波信号，这些信号经过处理（如快速傅里叶变换、特征提取等）后形成一个完整的数据帧。

2. **在该论文中的帧含义**：
   在论文的上下文中，“帧”是指通过UWB雷达获取的微多普勒签名数据片段，每个片段（或帧）反映了在特定时间窗口内目标的运动特征。这个时间窗口通常是非常短的时间段，与雷达的发射-接收周期有关。例如，在 0.18 秒内可能包含多个连续的雷达发射-接收周期，每个周期产生一个数据帧。

3. **数据集的帧构建**：
   对于数据集的构建来说，论文中的帧指的是每次雷达发射和接收一次信号后得到的时间-多普勒签名图像。数据集由这些帧组成，每一帧对应一个雷达测量周期的结果，这些结果记录了不同时间下人体姿态的变化情况。

### 总结：

在该论文中，“帧”确实可以理解为雷达发射并接收到信号后的一个时间段数据，也就是一次完整的雷达测量周期所采集到的信息。因此，每一帧代表的是雷达在某一个时间点上捕获到的所有反射信号数据，经过处理后用于人体姿态估计的分析。
***

在论文中，为了构建数据集进行训练和测试，关节点标注是通过一个外部的运动捕捉系统来进行的。具体来说，使用的是**Noitom Perception Neuron 3 (N3) 运动捕捉系统**来捕捉人体姿态的关节点位置，并将这些标注数据与通过SISO UWB雷达获取的微多普勒签名数据同步对齐。

### 关节点标注过程的具体步骤：

1. **使用外部运动捕捉系统进行标注**：
   - Noitom Perception Neuron 3 (N3) 是一个高精度的运动捕捉系统，能够实时捕捉人体的关节点数据。它通常由多个惯性测量单元（IMUs）组成，附着在人体的不同位置（如关节），能够精确捕捉人体的三维运动数据。

2. **数据同步与标注**：
   - 在构建数据集时，N3 运动捕捉系统与UWB雷达同时工作，N3 系统会实时记录人体的关节点位置，并生成相应的标注数据（如2D或3D关节点坐标）。
   - 这些标注数据与UWB雷达采集到的微多普勒签名数据进行时间同步对齐。每一帧微多普勒签名数据都会对应一个时间点的人体关节点标注，确保两者在时间维度上的准确对应。

3. **生成训练和测试数据集**：
   - 在数据集构建完成后，将同步对齐后的数据拆分为训练集和测试集。训练集中包含雷达数据及其对应的关节点标注，用于训练模型来识别和估计人体姿态；测试集则用于验证模型的性能和泛化能力。
   
4. **标注数据格式**：
   - 关节点标注通常以关节点的二维或三维坐标的形式存储。例如，一个2D姿态估计任务可能包含人体的多个关键点（如头部、肩膀、肘部、膝盖等）的(x, y)坐标信息。对每个时间帧数据进行标注，得到标注数据集。

### 总结：

论文中的关节点标注是通过**Noitom Perception Neuron 3 (N3) 运动捕捉系统**进行的，这个系统能够实时捕捉人体的关节点位置数据，并将其与UWB雷达的微多普勒签名数据进行同步对齐。这样生成的数据集包括了训练和测试所需的带有标注的关节点数据，用于模型的训练和性能验证。
***
**微多普勒签名（Micro-Doppler Signature, MD Signature）** 是雷达信号处理中的一个概念，用来描述由于目标的细微运动而引起的多普勒频移变化特征。这种特征可以揭示目标物体的运动模式和特征，如人体活动、机械振动、车辆轮胎旋转等。

### 微多普勒效应的原理：

1. **多普勒效应**：
   - 多普勒效应描述的是一个运动目标相对于雷达的径向速度对回波信号频率造成的变化。简而言之，当目标接近雷达时，回波信号的频率增加；当目标远离雷达时，频率降低。

2. **微多普勒效应**：
   - 当目标物体上有一些细小部分（如人体的手臂、腿、车辆的旋转部件等）进行微小的周期性运动时，这些细小部分会在多普勒频移的基础上产生额外的频率变化。这种由目标的复杂运动引起的附加频移变化被称为微多普勒效应。
   - 微多普勒效应不仅反映了物体的整体运动（如人体的行走或跑步），还捕捉了物体内部或附着部分的细节运动（如手臂的摆动、腿部的抬起、步态的变化等）。

### 微多普勒签名的形成：

- **信号处理**：雷达发送一个信号脉冲，并接收到从目标反射回来的信号。通过对回波信号进行处理（如快速傅里叶变换，FFT），可以获得距离-多普勒图像。
- **签名特征**：微多普勒签名是从距离-多普勒图像中提取的，它呈现为随时间变化的频率分布，通常以时间-多普勒图（Time-Doppler Plot）的形式表示。这个图像展示了由于目标细小运动引起的多普勒频移随时间的变化模式。

### 总结：

**微多普勒签名** 是由雷达探测到的目标的复杂运动模式引起的频率变化特征，可以用来揭示目标的运动细节和行为特征。通过分析微多普勒签名，可以实现对目标的识别、分类和监测等任务。
***
![[Pasted image 20240829194347.png]]
### 为什么论文中的图像不是完全对称的？

1. **实际运动特征的差异**：
    
    - 论文中的图像代表了不同类型的运动（如行走、挥拳、坐起等）。这些运动的多普勒特征并不总是对称的。例如，“行走”的动作会产生更明显的多普勒频率变化，这些变化取决于身体的各部分（如腿和手臂）朝向或远离雷达的相对速度。
    - 此外，目标的旋转或非直线运动（如挥拳或旋转身体）会在频谱中产生非对称的频率变化。
2. **数据处理的不同方式**：
    
    - 论文中的图像可能使用了特定的数据处理方法，例如去除了静态目标或低速运动的部分，只显示高频变化的部分，这样可以使图像更加清晰地展示目标的运动特征。
    - 数据处理可能还包括去噪或其他滤波处理，这些处理会影响谱图的对称性。

### 如何调整代码以获得非对称微多普勒谱图

你可以尝试以下方法来更接近论文中的图像：

1. **去除静态或低速目标**：
    
    - 你可以通过在傅里叶变换后去除或减少低频分量来去除静态或慢速目标的影响。这会使谱图显示目标的快速运动特征，可能更接近论文中的样子。
2. **调整滤波器和傅里叶变换参数**：
    
    - 尝试使用不同的时间窗口长度和滤波参数，以突出显示特定运动的特征变化。
3. **只显示一个方向的多普勒频率**：
    
    - 如果知道目标的主要运动方向（例如靠近或远离雷达），可以只显示一个方向的多普勒频率。这样，图像将不再对称。
***
预处理步骤包括以下几个方面：

1. **杂波抑制和噪声减少**：使用适应性滤波技术来消除回波中的杂波和噪声，以增强信噪比，减少不必要的干扰。
    
2. **傅里叶变换（FFT）应用**：首先沿快速时间维度（fast-time dimension）应用FFT，得到距离单元（Range bin）。然后沿慢时间维度（slow-time dimension）应用FFT，生成多个距离-多普勒图（Range-Doppler maps），用于计算目标距离和速度。
    
3. **时间-多普勒谱图生成**：将每个距离-多普勒图的范围单元（Range Bin）沿距离轴进行求和，生成时间长度为n帧的微多普勒签名图（时间-多普勒谱图）。
    
4. **使用窗口函数**：在信号处理过程中使用窗口函数，以减少谱泄漏和相关问题。
    
5. **微多普勒谱图处理**：论文使用快速傅里叶变换（FFT）大小为512的框架数为500帧，并使用汉明窗口（Hamming window）来处理雷达信号。每个微多普勒图代表的数据大约为0.52秒。


在雷达信号处理中，**杂波（clutter）**和**噪声（noise）**是指影响雷达信号质量的两种不同类型的干扰：

### 1. 杂波（Clutter）

- **定义**：杂波指的是雷达信号中来自非目标物体的反射，这些反射可能是由周围环境中的静态或移动物体（如地面、树木、建筑物、车辆等）引起的。杂波会掩盖或干扰实际目标的回波信号，使得目标的检测和识别变得更加困难。
  
- **特征**：
  - **静态杂波**：如地面反射、建筑物等，它们通常具有相对稳定的多普勒频移（多普勒频移为零或非常小）。
  - **动态杂波**：如树叶摆动、海浪、雨滴等，它们会随着时间的推移产生一定的多普勒频移，且多普勒频移不固定。

- **对雷达信号处理的影响**：
  - 杂波会导致虚假目标（假信号）出现在雷达信号中，使得实际目标更难以检测和跟踪。尤其在低信噪比条件下，杂波可能淹没真正目标的回波信号。

### 2. 噪声（Noise）

- **定义**：噪声是指信号中随机的、不相关的电子干扰或本底噪声，它是所有电子系统中的一种固有现象。雷达信号中的噪声可以来源于雷达接收器自身（如热噪声）或外部环境的随机电磁干扰。

- **特征**：
  - 噪声通常是不可预测的、随机的，其频谱分布通常是平坦的（白噪声），且在整个雷达接收的频带内均匀分布。
  - 噪声对信号的叠加往往是不可避免的，噪声强度会随着接收机带宽、温度、电子元件的特性等因素而变化。

- **对雷达信号处理的影响**：
  - 噪声会降低信号的信噪比（SNR），使得目标的检测和估计更加困难。在雷达系统中，噪声的存在使得目标的回波信号变得模糊和不明确。
  - 噪声会掩盖弱信号，导致检测性能下降和假警报增加。

### 杂波与噪声的区别：

- **来源不同**：杂波来源于非目标物体的反射（环境因素），而噪声来源于雷达系统自身的电子干扰或外部随机电磁干扰。
- **特征不同**：杂波通常是有规律的（静态或动态反射），而噪声通常是随机的、不可预测的。
- **处理方法不同**：杂波抑制需要专门的信号处理技术（如自适应滤波、距离门、动目标显示（MTI）等），而噪声则可以通过增加信号的强度、带宽优化、滤波等方式减小影响。

### 在论文中的应用：

在论文的雷达信号处理中，“杂波抑制和噪声减少”是预处理的关键步骤，通过自适应滤波和傅里叶变换技术，能够消除不相关的反射（杂波）和随机干扰（噪声），提高目标的信噪比，使得微多普勒特征更为清晰，便于后续的人体姿态估计模型进行准确的分析。

去除雷达信号中的杂波和噪声是提高目标检测和识别准确性的关键步骤。以下是一些常用的去除杂波和噪声的典型方法：

### 去除杂波的方法

1. **动目标显示（Moving Target Indication, MTI）滤波**：
   - **原理**：MTI滤波器主要用于检测移动目标，并抑制静态杂波。它通过将当前接收的回波信号与前一时刻的回波信号进行比较来识别移动目标，静态物体的回波会被相减消除。
   - **适用场景**：适用于有明显静态杂波的环境，如地面目标检测和海面目标检测。
   - **优点**：能够有效去除静态杂波。
   - **缺点**：对低速移动目标或运动较慢的目标效果不佳。

2. **自适应杂波滤波（Adaptive Clutter Filtering）**：
   - **原理**：自适应滤波器通过动态调整滤波参数来适应杂波环境的变化。常用的自适应滤波技术包括空域自适应处理（如空间自适应波束形成）和频域自适应处理（如子空间投影技术）。
   - **适用场景**：用于杂波环境变化复杂的场景，如多径效应显著或动态杂波密集的区域。
   - **优点**：适应性强，能够处理复杂的杂波环境。
   - **缺点**：计算复杂度高，实时性要求高。

3. **距离门（Range Gating）**：
   - **原理**：基于目标与雷达之间的距离来抑制杂波信号。在距离门内，只有特定范围内的回波信号才会被接收和处理，其他范围的信号被抑制。
   - **适用场景**：适用于杂波主要集中在某些特定距离（如近距离地面杂波）的场景。
   - **优点**：实现简单，可以有效去除特定范围内的杂波。
   - **缺点**：对目标与杂波之间距离接近的情况效果不佳。

4. **多普勒滤波（Doppler Filtering）**：
   - **原理**：利用目标和杂波在多普勒频移上的差异，通过对雷达信号进行多普勒滤波来抑制杂波。特别是动目标显示器（MTD）雷达，可以通过多普勒处理区分运动目标和静止杂波。
   - **适用场景**：适用于杂波和目标在多普勒频率上有明显区分的场景。
   - **优点**：能有效抑制静态杂波。
   - **缺点**：对目标和杂波的多普勒频移差异不明显时，效果有限。

5. **零中频抑制（Notch Filtering）**：
   - **原理**：在多普勒频谱上，静止或缓慢移动的杂波通常集中在零频率附近。零中频抑制方法通过在零频率位置设置一个“陷波”来去除这些杂波。
   - **适用场景**：适用于检测高速运动目标时，抑制低速杂波。
   - **优点**：实现简单，有效抑制低频杂波。
   - **缺点**：对低速运动目标效果不佳。

### 去除噪声的方法

1. **低通滤波器（Low-pass Filtering）**：
   - **原理**：低通滤波器允许通过低频信号，抑制高频噪声。典型的滤波器有Butterworth滤波器、Chebyshev滤波器等。
   - **适用场景**：适用于噪声主要集中在高频部分的场景。
   - **优点**：实现简单，可以有效去除高频噪声。
   - **缺点**：可能会削弱目标的高频特征。

2. **均值滤波和中值滤波（Mean and Median Filtering）**：
   - **原理**：均值滤波通过对信号进行平滑处理来减少噪声，而中值滤波通过计算局部数据的中值来去除孤立的噪声点。
   - **适用场景**：适用于信号被随机噪声污染的情况。
   - **优点**：能够去除尖峰噪声，保留信号边缘。
   - **缺点**：可能会导致细节信息丢失。

3. **维纳滤波（Wiener Filtering）**：
   - **原理**：维纳滤波是一种最小均方误差滤波器，它根据信号和噪声的统计特性，优化信号与噪声的分离。
   - **适用场景**：适用于已知信号和噪声的统计特征的情况。
   - **优点**：能在最大限度保留信号细节的情况下有效抑制噪声。
   - **缺点**：需要已知或估计信号和噪声的特性。

4. **多尺度分析（Multi-scale Analysis）**：
   - **原理**：通过小波变换等多尺度方法对信号进行分析，提取各尺度下的信号特征并去除噪声。
   - **适用场景**：适用于噪声具有不同尺度分布的情况。
   - **优点**：能够更好地提取信号的多尺度特征。
   - **缺点**：计算复杂度较高。

5. **卡尔曼滤波（Kalman Filtering）**：
   - **原理**：卡尔曼滤波是一种递归算法，通过预测和更新步骤对动态系统中的状态进行估计，减少噪声的影响。
   - **适用场景**：适用于目标运动轨迹连续且可以预测的情况。
   - **优点**：实时性好，能够对动态目标进行连续跟踪。
   - **缺点**：依赖于准确的系统模型和噪声统计特性。

### 总结

去除雷达信号中的杂波和噪声通常需要结合多种方法，根据具体应用场景的不同特征选择合适的技术。例如，在检测移动目标时，可能会使用动目标显示（MTI）滤波器和多普勒滤波；而对于信号的去噪处理，则可能选择维纳滤波或卡尔曼滤波，以保持信号的完整性和准确性。
***
在学术研究和深度学习过程中，数据预处理后的保存位置通常遵循以下几种常见的文件夹命名和组织方式：

### 常见的文件夹命名方式

1. **`processed` 或 `preprocessed` 文件夹**：
   - 这是最常见的命名方式之一。预处理后的数据通常会被保存到一个名为 `processed` 或 `preprocessed` 的文件夹中，以表明数据已经经过预处理步骤，并且可以直接用于模型训练或进一步分析。
   - 示例：`data/processed/` 或 `data/preprocessed/`

2. **`train`、`test` 和 `validation` 文件夹**：
   - 根据数据的用途进行分类，预处理后的数据可能被分为训练集、测试集和验证集，并分别存储在名为 `train`、`test` 和 `validation` 的文件夹中。这种命名方式特别适用于监督学习任务。
   - 示例：`data/train/`, `data/test/`, `data/validation/`

3. **`features` 或 `extracted_features` 文件夹**：
   - 如果数据预处理的结果是提取了特征（如通过特征工程或特征选择），那么这些特征数据通常会被保存到 `features` 或 `extracted_features` 文件夹中。
   - 示例：`data/features/` 或 `data/extracted_features/`

4. **`normalized`、`standardized` 文件夹**：
   - 如果预处理过程包括归一化或标准化等操作，那么数据可以存储在一个命名为 `normalized` 或 `standardized` 的文件夹中，以反映其具体的预处理步骤。
   - 示例：`data/normalized/`, `data/standardized/`

5. **`intermediate` 或 `temp` 文件夹**：
   - 在一些深度学习项目中，预处理数据的中间结果或临时文件可能会被存储在一个名为 `intermediate` 或 `temp` 的文件夹中。这些文件夹通常用于存储在最终结果生成之前的中间状态数据。
   - 示例：`data/intermediate/`, `data/temp/`
***


### Python代码：直达波抑制和自适应滤波

```python
import numpy as np
from scipy.signal import butter, filtfilt

# 定义Butterworth滤波器
def butter_lowpass(cutoff, fs, order=5):
    nyq = 0.5 * fs
    normal_cutoff = cutoff / nyq
    b, a = butter(order, normal_cutoff, btype='low', analog=False)
    return b, a

# 应用低通滤波器
def lowpass_filter(data, cutoff, fs, order=5):
    b, a = butter_lowpass(cutoff, fs, order=order)
    y = filtfilt(b, a, data)  # 使用filtfilt实现零相位滤波，减少边缘效应
    return y

# 加载数据
file_path = './data/__20_walk_1.txt'  # 替换为您的文件路径
data = []

with open(file_path, 'r') as file:
    for line in file:
        if line.startswith('x:'):
            x = float(line.split('x:')[1].strip())
            data.append(x)

# 转换为NumPy数组
data = np.array(data)

# 数据归一化
data = (data - np.mean(data)) / np.std(data)

# 设定滤波器参数
# 调整滤波器参数
cutoff = 10.0  # 较高的截止频率 (Hz) 以保留更多细节
fs = 100.0     # 采样率 (Hz)
order = 4      # 滤波器阶数，可根据需要调整

# 过滤数据
filtered_data = lowpass_filter(data, cutoff, fs, order)

# 反归一化（如果需要）
filtered_data = filtered_data * np.std(data) + np.mean(data)

# 输出结果
print("原始数据: ", data[:10])         # 显示前10个原始数据点
print("滤波后数据: ", filtered_data[:10])  # 显示前10个滤波后数据点

```
### 代码解释

1. **数据加载**：从指定的本地文件路径加载数据，将其解析为浮点数。
2. **Butterworth滤波器**：使用`butter`函数定义低通滤波器参数，包括截止频率和滤波器阶数。
3. **低通滤波**：应用低通滤波器以消除直达波和高频噪声。
4. **结果输出**：打印原始数据和滤波后的数据以比较滤波效果。
***

### 数据集的潜在缺陷

论文中使用的数据集确实存在一些潜在的局限性，特别是在以下几个方面：

1. **单一人物与静态物体场景**：
   - 论文中的数据集似乎只考虑了一个人在一个相对静态的环境中进行各种动作。虽然这可以为模型提供清晰的训练信号，并便于模型学习单个人的运动特征，但它并没有考虑到更复杂的实际情况。
   - 实际场景中可能会有多个目标（如多个人或非人物体）同时运动。模型在仅有单一人物数据的训练下，可能难以有效区分和处理多目标场景。

2. **缺少非人物体的运动数据**：
   - 论文中的数据集和预处理步骤专注于提取与人体运动相关的微多普勒特征，但没有考虑其他类型的运动物体（如宠物、挥动的物体等）的信号。这可能导致模型在实际应用中出现错误检测或误识别。例如，模型可能会将宠物的运动误认为是人的一部分。

### 论文中模型设计在复杂场景下的挑战

1. **多目标场景下的混淆**：
   - 当有多个目标同时活动时（如两个人走路、一个人走路和另一个人挥手等），他们的微多普勒信号会叠加在一起。在这种情况下，模型可能会混淆不同目标的特征，导致姿态估计错误。
   - 深度学习模型依赖于在训练过程中看到的场景和数据。如果模型没有在多目标或复杂场景中进行训练和验证，那么它在这种情况下的泛化能力可能会受限。

2. **非人物体的影响**：
   - 在实际应用中，雷达可能会捕捉到宠物、机械设备或其他物体的运动。这些物体的运动模式可能与人体的部分特征类似（如频率和速度），导致模型产生误检。

3. **缺乏多种复杂场景的数据增强**：
   - 数据集中的数据增强技术在模型的泛化能力中起到了关键作用。单一环境和有限场景的数据集可能导致模型在实际环境中表现不佳。需要更复杂的多目标数据集和更强的数据增强技术，以提升模型在不同环境下的表现。

### 如何改进模型和数据集

1. **扩展数据集的多样性**：
   - 数据集中应包括多种环境下的多目标场景，例如多个同时移动的人、人与宠物的组合运动等。这种多样性有助于模型学习更复杂的特征，并能够在多目标情况下有效分离各个目标的运动特征。

2. **引入非人物体的数据**：
   - 在数据集中加入各种非人物体的运动数据，如宠物、机械设备等，以增强模型的区分能力。模型可以学习到不同类型的物体与人体运动的差异，更好地区分不同来源的微多普勒特征。

3. **应用更复杂的目标分离和特征提取技术**：
   - 使用更高级的信号处理和机器学习方法，如多目标跟踪、多传感器融合等，来进一步提高模型在多目标和复杂场景下的表现。

### 总结

论文中的数据集和模型在简单场景下表现良好，但在更复杂的现实环境中可能会遇到挑战。要使模型在这些环境下具有更好的泛化能力，需要扩展数据集的多样性，并在训练过程中加入更多复杂场景和目标类型的数据。这些改进可以帮助模型更好地提取和分离运动特征，从而在更广泛的实际应用中表现更稳定和准确。
***
在多人场景下，利用雷达信号提取微多普勒特征的方法确实会面临更大的挑战，但它并不完全失效。学术界已经在多个方面研究了如何在复杂场景中提高微多普勒特征提取和目标识别的准确性。以下是应对多人场景的主要挑战和解决方案：

### 多人场景下微多普勒特征提取的挑战

1. **信号混叠**：
   - 当多人在同一场景中运动时，雷达接收到的微多普勒信号会叠加，导致不同人的信号混合在一起。这样，频率和时间特征会重叠，使得单个目标的特征变得模糊和难以分离。

2. **目标之间的相互干扰**：
   - 多个目标之间的相互靠近和相互遮挡会导致雷达的回波信号叠加和混淆，尤其是在雷达分辨率有限的情况下。这种情况会对目标的检测和跟踪造成挑战。

3. **复杂运动模式**：
   - 多目标场景中的复杂运动模式，如同时有多人在不同方向上行走、跑步或挥手等，这会在微多普勒谱图上形成复杂的时间-频率特征，进一步增加了目标分离的难度。

### 学术界的解决方案

1. **多输入多输出（MIMO）雷达**：
   - **原理**：MIMO雷达可以通过多个发射机和接收机组合，产生更高的角度分辨率和目标分辨能力。它可以在不同的角度和距离上区分多个目标的信号。
   - **优势**：提高了雷达的空间分辨率和目标区分能力，减少了多人运动信号的混叠问题。

2. **多目标跟踪（Multi-Target Tracking, MTT）算法**：
   - **原理**：MTT算法利用多个目标的动态模型和概率数据关联方法，跟踪场景中的多个目标。这些算法如卡尔曼滤波、多假设追踪（MHT）、联合概率数据关联（JPDA）等，能够估计每个目标的位置和速度。
   - **优势**：在动态场景中能够有效跟踪多个目标，提取每个目标的独立运动特征。

3. **稀疏分解和信号分离技术**：
   - **原理**：利用稀疏分解和盲源分离（如独立成分分析, ICA）方法，将混合信号分解成多个独立成分。基于这些技术，可以将来自不同目标的微多普勒特征分离出来。
   - **优势**：能够在单通道情况下有效分离多目标信号，减少信号混叠的影响。

4. **深度学习和时空特征学习**：
   - **原理**：使用卷积神经网络（CNN）、Transformer、长短时记忆网络（LSTM）等深度学习模型，学习复杂场景下的时空特征。通过结合微多普勒谱图的时间和频率特征，这些模型可以更好地区分不同目标的运动模式。
   - **优势**：具有强大的特征学习和泛化能力，能够在多人场景中识别复杂的运动模式。

5. **多模态融合**：
   - **原理**：结合雷达信号与其他传感器（如摄像头、LiDAR等）的数据，通过多模态融合提高对复杂场景的理解和目标识别能力。例如，利用雷达的速度检测能力与摄像头的高分辨率成像能力相结合。
   - **优势**：提高了多目标检测和识别的准确性，增强了系统的鲁棒性。

### 结论

虽然多人场景对微多普勒特征提取提出了更高的要求，但学术界已经开发了多种方法来应对这些挑战。通过改进雷达硬件（如MIMO雷达）、信号处理算法（如MTT和稀疏分解）、深度学习技术以及多传感器融合，这些方法可以在复杂的多人场景下保持较高的特征提取和目标识别能力。因此，在多人和复杂场景中，微多普勒特征提取并未失效，而是需要更先进的处理技术和算法支持。